{% extends 'base.html.twig' %}

{% block title %} {{product.name}}  - my shop{% endblock %}

{% block content %}

    <div class="row" style= "margin-bottom: 170px;">
        <div class="col-md-5">
            <img src=" {{vich_uploader_asset(product, 'imageFile')}} " class="img-fluid" width="350" height="350" alt=" {{product.name}} ">
        </div>

        <div class="col-md-7 my-auto">
            <h3> {{product.name}} </h3> 
            <p> {{product.subtitle|raw}} </p>
            <span class="single-price">{{(product.price/100)|number_format(2, ',', '.') }} € </span> 
            <hr>
            <p>
                {{product.description}}
            </p>
            <hr>
            <a href="" class="btn btn-primary"> Ajouter au panier</a>
        </div>
    </div>


{% endblock %}
