{% extends 'base.html.twig' %}

{% block title %}Mes adresses- myShop{% endblock %}

{% block content %}
    <div class="container">
        <a class="btn btn-info btn-sm float-right" href=" {{path('account_address_add')}} ">Ajouter une adresse</a>
        <h1>mes adresses</h1>
        <p>c'est dans cet espace que vous pourez gérer toutes vos adresses</p> 
        <a href="{{path('account')}}" c>retour</a>
        
        <hr>
        {% if app.user.addresses|length == 0 %}
            <h5 class="text-center">Vous n'avez pas encore renseigné d'adresses.Pour en ajouter une, <a href=" {{path('account_address_add')}} ">cliquez ici</a></h5>
        {% else %}
            <div class="row">
            {% for address in app.user.addresses %}
            <div class="col-md-4">
                <div class="card" style="width:300px">
                    <div class="card-body">
                        <h4 class="card-title"> {{address.name}} </h4> 
                        <p class="card-text">
                             {{address.address}} <br>
                             {{address.city}} - {{address.postal}} - {{address.country}}   
                        </p>
                        <a href=" {{path('account_address_edit', {id: address.id})}} " class="btn btn-primary">Modifier</a> |
                        <a href=" {{path('account_address_delete', {id: address.id})}} " class="btn btn-danger">Supprimer</a>
                    </div>
                </div>
            </div>   
            {% endfor %}
            </div>
        {% endif %}

    </div>
{% endblock %}
